<% provide(:title, "NEW EXPENSE") %>
<% if can? :manage, @expense %>
  <%= render "form", expense: @expense %>
<% else %>
  <h2>Not your GROUP. Go Back.</h2>
<% end %>
<%= link_to "Back to GROUP", group_path(@group), class: "btn-white" %>
