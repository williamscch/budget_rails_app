<% provide(:title, "EDIT GROUP") %>
<% if can? :manage, @group %>
  <%= render "form", group: @group %>
<% else %>
  <h2>Not your group. Go Back.</h2>
<% end %>
<%= link_to "Back to groups", groups_path, class: "btn-white" %>
